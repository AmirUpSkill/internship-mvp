# --- Python ---
# Virtual environments
venv/
*/venv/  # Catch venvs inside service folders if any were missed
.venv/
*/.venv/
env/
*/env/
# Compiled Python files
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Node.js ---
# Dependencies
node_modules/
*/node_modules/ # Catch node_modules inside service/front-end folders
# Build artifacts / caches
.next/
*/.next/
# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Lock files (if not using pnpm's lock file consistently, but generally pnpm.lock should be committed)
# package-lock.json # Uncomment if you exclusively use pnpm and want to ignore npm's lock
# yarn.lock # Uncomment if you exclusively use pnpm and want to ignore yarn's lock

# --- Environment Variables ---
# NEVER commit your secrets!
.env
*/.env
.env.*
*/.env.*
!.env.example # Make an exception so example files CAN be committed
!*/.env.example

# --- IDE / Editor / OS Files ---
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# --- Other ---
# Log files
*.log
# Add any other build outputs or temporary files specific to your tools